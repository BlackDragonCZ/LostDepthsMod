package cz.blackdragoncz.lostdepths.procedures;

import net.minecraft.world.level.levelgen.structure.templatesystem.StructureTemplate;
import net.minecraft.world.level.levelgen.structure.templatesystem.StructurePlaceSettings;
import net.minecraft.world.level.block.Rotation;
import net.minecraft.world.level.block.Mirror;
import net.minecraft.world.level.block.Blocks;
import net.minecraft.world.level.LevelAccessor;
import net.minecraft.server.level.ServerLevel;
import net.minecraft.resources.ResourceLocation;
import net.minecraft.core.BlockPos;

public class ManufactoryOnStructureInstanceGeneratedProcedure {
	public static void execute(LevelAccessor world, double x, double y, double z) {
		double x_now = 0;
		double z_now = 0;
		if (true) {
			if (true) {
				x_now = x + 8;
				z_now = z + 7;
				if (world instanceof ServerLevel _serverworld) {
					StructureTemplate template = _serverworld.getStructureManager().getOrCreate(new ResourceLocation("lostdepths", "manufactory_part1"));
					if (template != null) {
						template.placeInWorld(_serverworld, BlockPos.containing(x_now, y, z_now), BlockPos.containing(x_now, y, z_now), new StructurePlaceSettings().setRotation(Rotation.NONE).setMirror(Mirror.NONE).setIgnoreEntities(false),
								_serverworld.random, 3);
					}
				}
			}
			if (true) {
				x_now = x + 23;
				z_now = z + 7;
				if (world instanceof ServerLevel _serverworld) {
					StructureTemplate template = _serverworld.getStructureManager().getOrCreate(new ResourceLocation("lostdepths", "manufactory_part2"));
					if (template != null) {
						template.placeInWorld(_serverworld, BlockPos.containing(x_now, y, z_now), BlockPos.containing(x_now, y, z_now), new StructurePlaceSettings().setRotation(Rotation.NONE).setMirror(Mirror.NONE).setIgnoreEntities(false),
								_serverworld.random, 3);
					}
				}
			}
			if (true) {
				x_now = x + 38;
				z_now = z + 7;
				if (world instanceof ServerLevel _serverworld) {
					StructureTemplate template = _serverworld.getStructureManager().getOrCreate(new ResourceLocation("lostdepths", "manufactory_part3"));
					if (template != null) {
						template.placeInWorld(_serverworld, BlockPos.containing(x_now, y, z_now), BlockPos.containing(x_now, y, z_now), new StructurePlaceSettings().setRotation(Rotation.NONE).setMirror(Mirror.NONE).setIgnoreEntities(false),
								_serverworld.random, 3);
					}
				}
			}
			if (true) {
				x_now = x + 53;
				z_now = z + 7;
				if (world instanceof ServerLevel _serverworld) {
					StructureTemplate template = _serverworld.getStructureManager().getOrCreate(new ResourceLocation("lostdepths", "manufactory_part4"));
					if (template != null) {
						template.placeInWorld(_serverworld, BlockPos.containing(x_now, y, z_now), BlockPos.containing(x_now, y, z_now), new StructurePlaceSettings().setRotation(Rotation.NONE).setMirror(Mirror.NONE).setIgnoreEntities(false),
								_serverworld.random, 3);
					}
				}
			}
		}
		if (true) {
			if (true) {
				x_now = x + 8;
				z_now = z + 22;
				if (world instanceof ServerLevel _serverworld) {
					StructureTemplate template = _serverworld.getStructureManager().getOrCreate(new ResourceLocation("lostdepths", "manufactory_part5"));
					if (template != null) {
						template.placeInWorld(_serverworld, BlockPos.containing(x_now, y, z_now), BlockPos.containing(x_now, y, z_now), new StructurePlaceSettings().setRotation(Rotation.NONE).setMirror(Mirror.NONE).setIgnoreEntities(false),
								_serverworld.random, 3);
					}
				}
			}
			if (true) {
				x_now = x + 23;
				z_now = z + 22;
				if (world instanceof ServerLevel _serverworld) {
					StructureTemplate template = _serverworld.getStructureManager().getOrCreate(new ResourceLocation("lostdepths", "manufactory_part6"));
					if (template != null) {
						template.placeInWorld(_serverworld, BlockPos.containing(x_now, y, z_now), BlockPos.containing(x_now, y, z_now), new StructurePlaceSettings().setRotation(Rotation.NONE).setMirror(Mirror.NONE).setIgnoreEntities(false),
								_serverworld.random, 3);
					}
				}
			}
			if (true) {
				x_now = x + 38;
				z_now = z + 22;
				if (world instanceof ServerLevel _serverworld) {
					StructureTemplate template = _serverworld.getStructureManager().getOrCreate(new ResourceLocation("lostdepths", "manufactory_part7"));
					if (template != null) {
						template.placeInWorld(_serverworld, BlockPos.containing(x_now, y, z_now), BlockPos.containing(x_now, y, z_now), new StructurePlaceSettings().setRotation(Rotation.NONE).setMirror(Mirror.NONE).setIgnoreEntities(false),
								_serverworld.random, 3);
					}
				}
			}
			if (true) {
				x_now = x + 53;
				z_now = z + 22;
				if (world instanceof ServerLevel _serverworld) {
					StructureTemplate template = _serverworld.getStructureManager().getOrCreate(new ResourceLocation("lostdepths", "manufactory_part8"));
					if (template != null) {
						template.placeInWorld(_serverworld, BlockPos.containing(x_now, y, z_now), BlockPos.containing(x_now, y, z_now), new StructurePlaceSettings().setRotation(Rotation.NONE).setMirror(Mirror.NONE).setIgnoreEntities(false),
								_serverworld.random, 3);
					}
				}
			}
		}
		if (true) {
			if (true) {
				x_now = x + 8;
				z_now = z + 37;
				if (world instanceof ServerLevel _serverworld) {
					StructureTemplate template = _serverworld.getStructureManager().getOrCreate(new ResourceLocation("lostdepths", "manufactory_part9"));
					if (template != null) {
						template.placeInWorld(_serverworld, BlockPos.containing(x_now, y, z_now), BlockPos.containing(x_now, y, z_now), new StructurePlaceSettings().setRotation(Rotation.NONE).setMirror(Mirror.NONE).setIgnoreEntities(false),
								_serverworld.random, 3);
					}
				}
			}
			if (true) {
				x_now = x + 23;
				z_now = z + 37;
				if (world instanceof ServerLevel _serverworld) {
					StructureTemplate template = _serverworld.getStructureManager().getOrCreate(new ResourceLocation("lostdepths", "manufactory_part10"));
					if (template != null) {
						template.placeInWorld(_serverworld, BlockPos.containing(x_now, y, z_now), BlockPos.containing(x_now, y, z_now), new StructurePlaceSettings().setRotation(Rotation.NONE).setMirror(Mirror.NONE).setIgnoreEntities(false),
								_serverworld.random, 3);
					}
				}
			}
			if (true) {
				x_now = x + 38;
				z_now = z + 37;
				if (world instanceof ServerLevel _serverworld) {
					StructureTemplate template = _serverworld.getStructureManager().getOrCreate(new ResourceLocation("lostdepths", "manufactory_part11"));
					if (template != null) {
						template.placeInWorld(_serverworld, BlockPos.containing(x_now, y, z_now), BlockPos.containing(x_now, y, z_now), new StructurePlaceSettings().setRotation(Rotation.NONE).setMirror(Mirror.NONE).setIgnoreEntities(false),
								_serverworld.random, 3);
					}
				}
			}
			if (true) {
				x_now = x + 53;
				z_now = z + 37;
				if (world instanceof ServerLevel _serverworld) {
					StructureTemplate template = _serverworld.getStructureManager().getOrCreate(new ResourceLocation("lostdepths", "manufactory_part12"));
					if (template != null) {
						template.placeInWorld(_serverworld, BlockPos.containing(x_now, y, z_now), BlockPos.containing(x_now, y, z_now), new StructurePlaceSettings().setRotation(Rotation.NONE).setMirror(Mirror.NONE).setIgnoreEntities(false),
								_serverworld.random, 3);
					}
				}
			}
		}
		if (true) {
			if (true) {
				x_now = x + 8;
				z_now = z + 52;
				if (world instanceof ServerLevel _serverworld) {
					StructureTemplate template = _serverworld.getStructureManager().getOrCreate(new ResourceLocation("lostdepths", "manufactory_part13"));
					if (template != null) {
						template.placeInWorld(_serverworld, BlockPos.containing(x_now, y, z_now), BlockPos.containing(x_now, y, z_now), new StructurePlaceSettings().setRotation(Rotation.NONE).setMirror(Mirror.NONE).setIgnoreEntities(false),
								_serverworld.random, 3);
					}
				}
			}
			if (true) {
				x_now = x + 23;
				z_now = z + 52;
				if (world instanceof ServerLevel _serverworld) {
					StructureTemplate template = _serverworld.getStructureManager().getOrCreate(new ResourceLocation("lostdepths", "manufactory_part14"));
					if (template != null) {
						template.placeInWorld(_serverworld, BlockPos.containing(x_now, y, z_now), BlockPos.containing(x_now, y, z_now), new StructurePlaceSettings().setRotation(Rotation.NONE).setMirror(Mirror.NONE).setIgnoreEntities(false),
								_serverworld.random, 3);
					}
				}
			}
			if (true) {
				x_now = x + 38;
				z_now = z + 52;
				if (world instanceof ServerLevel _serverworld) {
					StructureTemplate template = _serverworld.getStructureManager().getOrCreate(new ResourceLocation("lostdepths", "manufactory_part15"));
					if (template != null) {
						template.placeInWorld(_serverworld, BlockPos.containing(x_now, y, z_now), BlockPos.containing(x_now, y, z_now), new StructurePlaceSettings().setRotation(Rotation.NONE).setMirror(Mirror.NONE).setIgnoreEntities(false),
								_serverworld.random, 3);
					}
				}
			}
			if (true) {
				x_now = x + 53;
				z_now = z + 52;
				if (world instanceof ServerLevel _serverworld) {
					StructureTemplate template = _serverworld.getStructureManager().getOrCreate(new ResourceLocation("lostdepths", "manufactory_part16"));
					if (template != null) {
						template.placeInWorld(_serverworld, BlockPos.containing(x_now, y, z_now), BlockPos.containing(x_now, y, z_now), new StructurePlaceSettings().setRotation(Rotation.NONE).setMirror(Mirror.NONE).setIgnoreEntities(false),
								_serverworld.random, 3);
					}
				}
			}
		}
		world.setBlock(BlockPos.containing(x, y, z), Blocks.AIR.defaultBlockState(), 3);
	}
}
